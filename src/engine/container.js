// Defines and appends the primary container for the application to the document body.

const container = document.createElement('div');
document.body.appendChild(container);

module.exports = container;
